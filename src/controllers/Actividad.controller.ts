import { Request, Response } from "express";
import { Actividad } from "../models/Actividad"

export const AgregarActividad = async (req: Request, res: Response) => {
    /////PONGAN EL CODIGO AQUI PARA AGREGAR/EDITAR UNA IMAGEN.
};

export const BorrarActividad = async (req: Request, res: Response) => {
    /////PONGAN EL CODIGO AQUI PARA AGREGAR UNA IMAGEN.
};

export const ConseguirActividades = async (req: Request, res: Response) => {
    /////PONGAN EL CODIGO AQUI PARA AGREGAR UNA IMAGEN.
};

